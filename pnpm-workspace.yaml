# Allow these dependencies to run their install/build scripts (pnpm v11 blocks
# them by default). esbuild needs its postinstall to fetch its native binary,
# without which the Vite build fails.
allowBuilds:
  '@parcel/watcher': true
  esbuild: true
  sharp: true
